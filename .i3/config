# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# font for window titles. ISO 10646 = Unicode
#font xft:lime:pixelsize=11
font pango:Source Code Pro 10

# colors

#class          	 border   backgr.  text
client.focused           #121212  #101010  #9e8a8e
client.focused_inactive  #101010  #191919  #999999
client.unfocused         #101010  #191919  #999999
client.urgent            #cd989a  #cd989a  #2e3436

client.background     	#1d1d1d

force_xinerama yes

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec urxvtc

# kill focused window
bindsym $mod+Shift+A kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run -fn  "-*-profont-*-*-*-*-11-*-*-*-*-*-*-*" -nb "#161616" -nf "#909737" -sb "#909737" -sf "#161616"

# change focus
bindsym $mod+Left  focus left
bindsym $mod+Down  focus down
bindsym $mod+Up    focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+Left  move left
bindsym $mod+Shift+Down  move down
bindsym $mod+Shift+Up    move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+q focus parent

# focus the child container
#bindcode $mod+d focus child

# Media keys
bindsym XF86AudioPlay        exec ncmpcpp toggle
bindsym XF86AudioStop        exec ncmpcpp stop
bindsym XF86AudioPrev        exec ncmpcpp prev
bindsym XF86AudioNext        exec ncmpcpp next
bindsym XF86WebCam           exec cheese

set $WS01 "1:  Terms"
set $WSweb "2:  Web"
set $WSDev "3:  Dev"
set $WSFiles "4:  Files"
set $WSirc "5:  IRC"
set $WS06 "6:  "
set $WS07 "7:  "
set $WS08 "8:  "
set $WS09 "9:  "
set $WS10 "10:  "

# switch to workspace
bindsym $mod+ampersand  workspace $WS01
bindsym $mod+eacute     workspace $WSweb
bindsym $mod+quotedbl   workspace $WSDev
bindsym $mod+apostrophe workspace $WSFiles
bindsym $mod+parenleft  workspace $WSirc
bindsym $mod+minus      workspace $WS06
bindsym $mod+egrave     workspace $WS07
bindsym $mod+underscore workspace $WS08
bindsym $mod+ccedilla   workspace $WS09
bindsym $mod+agrave     workspace $WS10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $WS01
bindsym $mod+Shift+2 move container to workspace $WSweb
bindsym $mod+Shift+3 move container to workspace $WSDev
bindsym $mod+Shift+4 move container to workspace $WSFiles
bindsym $mod+Shift+5 move container to workspace $WSirc
bindsym $mod+Shift+6 move container to workspace $WS06
bindsym $mod+Shift+7 move container to workspace $WS07
bindsym $mod+Shift+8 move container to workspace $WS08
bindsym $mod+Shift+9 move container to workspace $WS09
bindsym $mod+Shift+0 move container to workspace $WS10

bindsym $mod+z workspace back_and_forth
bindsym $mod+Shift+z move container to workspace back_and_forth; workspace back_and_forth

bindsym $mod+m exec i3-input -F 'mark %s' -P 'Mark name: '
bindsym $mod+Shift+m exec i3-input -F '[con_mark=%s] focus' -P 'Go to mark: '

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

# Start a filemanager
bindsym $mod+x exec xterm -e ranger
bindsym $mod+Shift+x exec nautilus --no-desktop

# Brightness control
bindsym XF86MonBrightnessDown exec "xbacklight -dec 10"
bindsym XF86MonBrightnessUp exec "xbacklight -inc 20"

# Volume ALSA
#bindsym XF86AudioMute exec --no-startup-id "amixer set Master toggle; pkill -RTMIN+10 i3blocks"
#bindsym XF86AudioRaiseVolume exec --no-startup-id "amixer set Master 5%+ unmute; pkill -RTMIN+10 i3blocks; ogg123 /usr/share/sounds/freedesktop/stereo/audio-volume-change.oga"
#bindsym XF86AudioLowerVolume exec --no-startup-id "amixer set Master 5%- unmute; pkill -RTMIN+10 i3blocks; ogg123 /usr/share/sounds/freedesktop/stereo/audio-volume-change.oga"

# Volume PULSE
bindsym XF86AudioMute exec --no-startup-id "amixer -D pulse set Master toggle; pkill -RTMIN+10 i3blocks"
bindsym XF86AudioRaiseVolume exec --no-startup-id "amixer -D pulse set Master 5%+ unmute; pkill -RTMIN+10 i3blocks; ogg123 /usr/share/sounds/freedesktop/stereo/audio-volume-change.oga"
bindsym XF86AudioLowerVolume exec --no-startup-id "amixer -D pulse set Master 5%- unmute; pkill -RTMIN+10 i3blocks; ogg123 /usr/share/sounds/freedesktop/stereo/audio-volume-change.oga"



# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym m resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

workspace_auto_back_and_forth yes

bindsym $mod+r mode "resize"

bindsym $mod+Shift+v [class="urxvt" title="VIM"] focus
bindsym $mod+l exec i3lock -t -i /home/baboon/wallpapers/wallhaven-178187.png

new_window 1pixel


for_window [instance="urxvt"]        border     normal
for_window [instance="Download"]     floating   enable
for_window [class="Tilda"]           floating   enable
for_window [class="mplayer"]         floating   enable
for_window [class="feh"]             floating   enable
for_window [class="urxvt"]           border     normal
for_window [class="emulator64-arm"]  border     normal
for_window [class="emulator64-arm"]  floating   enable

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
  mode dock
  position top 
  font pango:Source Code Pro 10
  status_command "i3blocks"
  
  colors {
      background #101010
      statusline #999999
      #focused_workspace  #101010 #191919 #598269
      focused_workspace   #101010 #191919 #30A13D
      active_workspace    #101010 #252525 #598269
      inactive_workspace  #101010 #191919 #30A13D
      urgent_workspace    #101010 #2e3436 #879A8F
    }
}

#exec --no-startup-id /usr/bin/xscreensaver -no-splash
exec --no-startup-id nm-applet
exec --no-startup-id mpd
#exec --no-startup-id g15aiostats
exec --no-startup-id dunst
#exec --no-startup-id compton -b --config ~/.compton.conf
exec --no-startup-id [ -f ~/.fehbg ] && ~/.fehbg
exec --no-startup-id parcellite -d -n
exec --no-startup-id urxvtd -q -o -f
exec --no-startup-id mpd-notification
